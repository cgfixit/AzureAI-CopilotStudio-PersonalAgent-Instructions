# Codex Repository Instructions

Use `.codex/AGENTS.md` as the repo-local source of truth for Codex workflow,
skills, commands, and verification.

Before committing Markdown or prompt changes, run:

```bash
python .codex/scripts/repo_audit.py preflight .
```
